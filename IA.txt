etapa 1:

prompt: en base al contexo que es "un refugio de animales quiere reemplazar su mural impreso por un directorio digital interactivo de mascotas en adopcion, exhibido en una
pantalla de la recepcion. El equipo del refugio te entrega el listado de mascotas y te pide una aplicacion web SPA en REACT, que sea simple y ordenada al igual que segura, 
que permite ver mascotas distinguir su especie y resalta los casos en 
adopcion urgente." en base a los elementos de REACT que son componente, JSX, props, estado(use state), renderizado de listas (.map + key)
y renderizado condicional puedes decirme para cada uno ¿donde lo uso en este problema? y ¿porque es adecuado?

utilidad: la respuesta de la ia fue muy util debido a que me entrega las respuestas para cada elemento que se encontraba en la tabla en base al contexto

modificacion: no modifique nada 

etapa 2: 

prompt: El componente recibe por props: nombre, raza, edad, especie, descripcion, caracteristicas 
y muestra la información de forma visual y ordenada
aplica un estilo diferenciado según la especie (un color/etiqueta distinto para Perro, Gato, Otro). 
Define los tipos de las props con PropTypes (o documenta claramente cada prop) y entrega valores por defecto para evitar errores
 cuando falte un dato. Renderiza el texto siempre como contenido JSX normal. 
 No uses dangerouslySetInnerHTML. No dejes credenciales, claves ni datos sensibles escritos en el código.

 utilidad: fue demasiado util debido a que me permitio poder crear el mastercard en base a los contenidos necesarios que necesitaba este
 al igual que crear un css para su estilo

 modificacion: no modifique nada

etapa 3:

prompt: Recorre el array con .map() y renderiza un MascotaCard por cada mascota. 
Asigna correctamente la prop key usando el id de cada elemento (no uses el índice del array).
 En App.jsx, renderiza ListaMascotas pasándole los datos importados.

utilidad: muy util debido a que me permitio colocar los archivos de listaMascotas.jsx de forma limpia y bien estructurada al igual que
en app.jsx renderizo los componentes de ListaMascotas

modificacion: no modifique nada

etapa 4.

prompt: crear botones o un selector para filtrar por la especie: “Todas”, “Perro”, “Gato”, “Otro”. 
En App.jsx, usa useState para guardar el filtro seleccionado y un campo de búsqueda por nombre. 
Aplica ambos filtros sobre el array antes de pasarlo a ListaMascotas. 
Usa renderizado condicional para mostrar el mensaje “No hay mascotas que coincidan” cuando el resultado quede vacío. 
El campo de búsqueda debe ser un componente controlado (su valor vive en el estado de React). 
Valida y normaliza el texto de búsqueda (recorta espacios y limita el largo) antes de usarlo,
 para no confiar ciegamente en la entrada del usuario.

 utilidad: muy util debido a que en filtro especie muestra botones para seleccionar el filtro de especie, filtra las mascotas
 segun la especie seleccionada y guarda el texto del campo de busqueda

 modificacion: no le modifique nada al resultadoo

 etapa 5:

 prompt: Resalta visualmente los elementos con adopcionUrgente: true (por ejemplo, una etiqueta “URGENTE” o un borde distinto). 
 Agrega un contador que muestre cuántas mascotas tienen adopción urgente. 
 Verifica que toda la app corre sin errores ni warnings en consola.

 utilidad: fue muy util debido a que Se agregó la prop adopcionUrgente a MascotaCard, se añadió un borde rojo y una etiqueta "URGENTE" para las mascotas con adopcionUrgente: true.
 Se calculan las mascotas urgentes en App.jsx y se muestra el número en la interfaz, entre otras cosas.

